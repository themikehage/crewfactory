services:
  crewfactory:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=change-me-in-production
      - AUTH_USERNAME=admin
      - AUTH_PASSWORD_HASH=JDJhJDEwJGVBV2JvSWIzeVZleWFWQk45QldMeS5EaHA2ZkhJaTJmUEx0SllxOVBPcFRtbW8zQ3BReDhx
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - crewfactory-data:/tmp/crewfactory

volumes:
  crewfactory-data:
